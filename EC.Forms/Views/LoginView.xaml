<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EC.Forms.Views.LoginView"
              xmlns:local="clr-namespace:EC.Forms.Views;assembly=EC.Forms" Title="LOGIN" BackgroundColor="Gray" >
  <!--<ContentPage.Resources>
    <ResourceDictionary>
      <Style TargetType="Entry">
        <Setter Property="BackgroundColor" Value="White"/>
        <Setter Property="HeightRequest" Value="10"/>
        <Setter Property="TextColor" Value="Gray"/>
      </Style>
    </ResourceDictionary>
  </ContentPage.Resources>-->



  <ScrollView >
   <StackLayout   HorizontalOptions="Center" VerticalOptions="Center" Padding="2">

      
        
        <Image  Source="logo.png"  Aspect="AspectFit " HeightRequest="300" />
 
        <local:CustonEmtry Placeholder="Email" Text="{Binding Email}" PlaceholderColor="White" Keyboard="Email"  TextColor="Teal"/>

        <local:CustonEmtry Placeholder="Password" Text="{Binding Password}"  PlaceholderColor="White" TextColor="Teal"  />


        <Button  TextColor="White" Text="Login" BorderColor="Teal" Command="{Binding LoginCommand}" HorizontalOptions="Center" />


      </StackLayout>
 
  </ScrollView>

</ContentPage>