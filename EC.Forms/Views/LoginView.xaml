<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EC.Forms.Views.LoginView"
              xmlns:local="clr-namespace:EC.Forms.Views;assembly=EC.Forms" Title="LOGIN" BackgroundImage="bggrass.jpg"  >
  <!--<ContentPage.Resources>
    <ResourceDictionary>
      <Style TargetType="Entry">
        <Setter Property="BackgroundColor" Value="White"/>
        <Setter Property="HeightRequest" Value="10"/>
        <Setter Property="TextColor" Value="Gray"/>
      </Style>
    </ResourceDictionary>
  </ContentPage.Resources>-->



  <ScrollView >
   <StackLayout   HorizontalOptions="Center" VerticalOptions="Center">

      
        
        <Image  Source="logo.png"  Aspect="AspectFit " HeightRequest="300" />
 
        <local:CustonEmtry Placeholder="Email" Text="{Binding Email}"  TextColor="White"/>

        <local:CustonEmtry Placeholder="Password" Text="{Binding Password}" TextColor="White"  />


        <local:ButtonTextCentered  Text="GO!" x:Name="LoginButton" TextColor="White" BackgroundColor="#de1e24" Command="{Binding LoginCommand}"/>

      </StackLayout>
 
  </ScrollView>

</ContentPage>